import { DragSortableDirective } from './drag-sortable.directive';

describe('DragSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new DragSortableDirective();
    expect(directive).toBeTruthy();
  });
});
